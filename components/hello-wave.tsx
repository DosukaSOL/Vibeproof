/**
 * HelloWave — unused Expo template component (Reanimated removed)
 */
import React from "react";
import { Text } from "react-native";

export function HelloWave() {
  return <Text style={{ fontSize: 28 }}>👋</Text>;
}
